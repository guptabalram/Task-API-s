/// <reference types="node" />
export = Inspector;
declare class Inspector {
    constructor(config?: {});
    session: inspector.Session;
    profiler: Profiler;
    heap: Heap;
    getCurrentSession(): inspector.Session;
    destroy(): Promise<void>;
}
import inspector = require("inspector");
import Profiler = require("./src/profiler");
import Heap = require("./src/heap");
